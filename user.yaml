- name: configure user server 
  hosts: user
  become: yes
  tasks:
  - name: disable latest version and enabled old version 
    ansible.builtin.command: dnf module disable nodejs -y
    ansible.builtin.command: dnf module enable nodejs:20 -y
  
  - name: install nodejs 
    ansible.builtin.dnf:
      name: nodejs
      state: present

  - name: create system user 
    ansible.builtin.user:
      name: roboshop
      system: true
      shell: /sbin/nologin
      home: /app

  - name: create wok directory 
    ansible.builtin.file:
      path: /app 
      state: directory 

  - name: download user code
    ansible.builtin.get_url:
       url: https://roboshop-artifacts.s3.amazonaws.com/user-v3.zip 
       dest: /tmp/user.zip

  - name: unzip the user code 
    ansible.builtin.unarchive:
      src: /tmp/user.zip
      dest: /app 
      remote_src: yes 

  - name: install the dependencies
    community.general.npm:
      path: /app

  - name: setup systemd service 
    ansible.builtin.copy:
      src: user.service
      dest: /etc/systemd/system/user.service 

  - name: reload the systemctl deamon
    ansible.builtin.systemd_service:
       daemon_reload: true

  - name: enable and start the user
    ansible.builtin.service:
      name: catalogue
      state: started
      enabled: yes 
